bool findPattern(string p, string s)
{
    int n =s.size();
    int m = p.size();
    string str ="";
    for(int i=0;i<n-m+1;i++){
        str = s.substr(i,m);
        if(str==p)return true;
    }
    return false;
}
