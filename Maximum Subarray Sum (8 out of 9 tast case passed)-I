#include <bits/stdc++.h> 
long long maxSubarraySum(int arr[], int n)
{
    int maxS = INT_MIN;
    int currS=0;
    for(int i=0;i<n;i++){
        currS=currS+arr[i];
        maxS=max(maxS,currS);
        if(currS<0)currS=0;
    }
    if(maxS<0)return 0;
    return maxS;
}
